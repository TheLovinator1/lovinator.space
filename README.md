# lovinator.space

This is the source code for my personal website, [lovinator.space](https://lovinator.space).

```bash
# Create secret key with Django
uv run python -c "from django.core.management import utils; print(utils.get_random_secret_key())"
```
